# Organizer Raycast Extension

## Installation

1. Clone this repository
2. Run `npm install` to install dependencies
3. Run `npm run dev` to start development
4. In extension settings, set the API URL to your Organizer API endpoint


## Configuration

The extension uses the following preferences:

- **API URL**: Base URL for the Organizer API 

